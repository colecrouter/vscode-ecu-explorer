{
	"name": "@ecu-explorer/core",
	"version": "0.0.1",
	"type": "module",
	"exports": {
		".": {
			"import": "./dist/index.js",
			"types": "./dist/index.d.ts"
		}
	},
	"scripts": {
		"build": "tsc",
		"check": "tsc --noEmit",
		"test": "vitest run",
		"test:ui": "vitest --ui",
		"test:coverage": "vitest run --coverage",
		"test:debug": "vitest --inspect-brk --inspect --single-thread"
	},
	"dependencies": {
		"expr-eval": "^2.0.2"
	},
	"devDependencies": {
		"typescript": "*",
		"@types/vscode": "*",
		"@types/vscode-webview": "*"
	}
}
